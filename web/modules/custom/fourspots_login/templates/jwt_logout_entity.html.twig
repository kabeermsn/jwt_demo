{#
/**
 * @file jwt_logout_entity.html.twig
 * Default theme implementation to present Jwtlogout entity data.
 *
 * This template is used when viewing Jwtlogout entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_jwt_logout_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('jwt_logout_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
